import { useState, type FormEvent } from "react"; import { Link } from "react-router-dom"; import { Alert } from "@/components/ui/Alert"; import { Button } from "@/components/ui/Button"; import { Input, Label } from "@/components/ui/Form"; import { apiPost } from "@/lib/api"; import { AuthShell } from "./AuthShell";
export default function ForgotPasswordPage() { const [sent, setSent] = useState(false); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const data = new FormData(e.currentTarget); await apiPost("/auth/forgot-password", { email: data.get("email") }); setSent(true); } return <AuthShell title="Reset your password" description="Enter your email and we'll send a secure reset link." path="/forgot-password" footer={<Link className="font-semibold text-[var(--color-brand-blue)]" to="/login">Back to sign in</Link>}>{sent ? <Alert variant="success">If an account exists for that address, a reset link has been sent.</Alert> : <form onSubmit={submit} className="space-y-5"><div><Label htmlFor="email">Email</Label><Input id="email" name="email" type="email" autoComplete="email" required /></div><Button className="w-full">Send reset link</Button></form>}</AuthShell>; }
